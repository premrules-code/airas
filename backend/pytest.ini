[pytest]
testpaths = tests
addopts = -p no:asyncio
markers =
    unit: fast unit tests
    integration: mocked integration tests
    scoring: agent scoring consistency
    evals: per-agent eval tests
